# Refined Request

## Original Request

현재 구현커밋 스킬 조사해서, 코덱스 플로우 기능에서 워크트리 만드는거 해제해주거나, 다 끝난거 알아서 없애게 바꾸는 쪽으로. `$요청개선`, `$research`, `$plan-first-implementation`을 사용해 계획까지만 수립한다.

## Refined Execution Request

`구현커밋` 스킬과 Codex Flow 런타임에서 격리 Git worktree를 생성·재사용·보존·정리하는 전체 제어 흐름을 조사한다. 불필요한 worktree 누적과 완료된 실행 공간의 재생성을 방지하기 위해, (A) worktree 격리를 기본적으로 비활성화하는 정책과 (B) 격리는 유지하되 성공적으로 통합·종료된 worktree를 자동 정리하는 정책을 포함해 최소 세 가지 설계 대안을 비교한다. 현재의 사용자 변경 보호, 실패 증거 보존, 재개·복구, branch finalization 계약을 훼손하지 않는 권고안을 선택하고, 실제 구현은 하지 않은 채 `research.md`와 실행 가능한 plan-first 계획 문서를 작성한다.

## Working Brief

- Intent: Codex Flow가 만든 보조 worktree가 누적되거나 완료 후 남는 문제를 구조적으로 예방한다.
- Work type: 코드베이스 조사, 동작·위험 분석, 대안 비교, 구현 계획 수립.
- Target: `/Users/moonsoo/projects/codex-flow` 런타임과 `/Users/moonsoo/projects/codex-skills-user/구현커밋/SKILL.md` 계약 및 관련 테스트·문서.
- Constraints: 계획까지만 수행한다. 런타임 코드, 스킬 계약, Git branch/worktree, 현재 실행 상태는 변경하거나 정리하지 않는다. 사용자 변경, 실패 후보, 재개 증거를 자동 삭제하는 설계를 채택하지 않는다.
- Success check: 생성·정리 호출 경로와 상태 전이를 파일/함수/테스트 근거로 특정하고, 세 가지 이상 대안을 비교한 `research.md`와 Commit/Phase 단위 계획 문서를 제공한다. 계획에는 성공·중단 조건, 제안 코드 스니펫, 회귀 테스트, Operator 결정 게이트가 포함되어야 한다.
- Routing: `요청개선` → `research`(Pre-Plan Research Gate) → `plan-first-implementation`. 후행 구현은 이번 요청 범위 밖이며 승인 시 `구현커밋`으로 넘긴다.

## Execution Notes

- Execution mode: refine-then-execute
- Artifact role: source prompt for the following research and planning work
- Chat response: artifact link plus a concise research/plan result
- Review artifact classification: plan-only operational/backend change; HTML is not required
- Write scope for this turn: request artifact, `research.md`, and plan-first Markdown only

## Execution Result

Completed the requested research-and-planning-only workflow.

- Research: `docs/plans/codex-flow-worktree-lifecycle/research.md`
- Plan: `docs/plans/codex-flow-worktree-lifecycle/plan.md`
- Recommendation: retain managed isolation, but change the default from eager creation to lazy creation; park clean inactive worktrees while preserving their branches; finalize worktrees and owned branches idempotently after verified merge.
- Rejected default: global in-place execution, because it weakens dirty-source isolation and held-candidate recovery.
- Baseline verification: focused route/worktree/merge tests passed (`28 passed in 8.24s`) using the existing Chat-Bot virtual environment. No runtime, skill contract, branch, or existing worktree was modified.
